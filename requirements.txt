# Core numerics — P0, must install
numpy>=1.26,<3
pandas>=2.2
scipy>=1.13
pyarrow>=16
duckdb>=1.1
scikit-learn>=1.5
xgboost>=2.1

# API — P0
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.5
httpx>=0.27

# Local agent — P0 (Needle 2, 14MB binary, Apache-2.0)
cactus-needle>=0.1

# Domain libraries — P1 (graceful degradation if absent)
pvlib>=0.11
ruptures>=1.1

# Dev
pytest>=8.3
ruff>=0.7
